{
  "name": "marver-gtm-templates",
  "version": "0.1.0",
  "private": true,
  "description": "GTM Community Templates for Marver (Data Workbench)",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "@playwright/test": "^1.45.0"
  }
}
